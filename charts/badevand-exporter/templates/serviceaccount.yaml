apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "badevand-exporter.serviceAccountName" . }}
  labels:
    {{- include "badevand-exporter.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
